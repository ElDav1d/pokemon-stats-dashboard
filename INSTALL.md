# Installation

I recommend using FNM and to have Corepack enabled on node to automatically use PNPM too as package manager.

## Install dependencies

`pnpm install` or `pnpm i`

## Start developing

`pnpm dev`

If you use another package managers you can use `bun i && bun dev` or `npm i && npm dev`
